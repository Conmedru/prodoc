name: Scrape Prodoctorov Doctors

on:
  # Manual trigger (workflow_dispatch)
  workflow_dispatch:
    inputs:
      city:
        description: 'City slug (default: moskva)'
        required: false
        default: 'moskva'
      sample_only:
        description: 'Run sample only (20 doctors) vs full pilot'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      resume_file:
        description: 'Excel file to resume from (e.g. doctors_20260302_115654.xlsx)'
        required: false
        default: 'doctors_20260302_115654.xlsx'
        type: string

  # Schedule trigger (uncomment to run automatically)
  # schedule:
  #   - cron: '0 2 * * 0'  # Weekly on Sunday at 2 AM UTC

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 hours max

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run scraper (sample)
        if: github.event.inputs.sample_only == 'true'
        env:
          ZENROWS_API_KEY: ${{ secrets.ZENROWS_API_KEY }}
        run: |
          python sample_20.py

      - name: Run scraper (full pilot)
        if: github.event.inputs.sample_only != 'true'
        env:
          ZENROWS_API_KEY: ${{ secrets.ZENROWS_API_KEY }}
        run: |
          if [ -n "${{ github.event.inputs.resume_file }}" ] && [ -f "${{ github.event.inputs.resume_file }}" ]; then
            python main.py --resume-excel "${{ github.event.inputs.resume_file }}"
          else
            python main.py
          fi

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: doctors-excel-${{ github.run_id }}
          path: |
            output/*.xlsx
            output/checkpoints/*.json
          retention-days: 30

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-${{ github.run_id }}
          path: |
            *.log
            scraper.log
          retention-days: 7
